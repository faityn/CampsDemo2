'use client'
import {motion,useScroll,useTransform} from 'framer-motion'
import {useRef} from 'react'
export default function ParallaxStory(){const ref=useRef<HTMLElement>(null);const {scrollYProgress}=useScroll({target:ref,offset:['start end','end start']});const y=useTransform(scrollYProgress,[0,1],[-80,80]);return <section ref={ref} className="relative min-h-[85vh] overflow-hidden bg-pine text-white noise"><motion.div style={{y}} className="absolute -inset-y-20 inset-x-0 bg-[url('https://images.unsplash.com/photo-1500534623283-312aade485b7?auto=format&fit=crop&w=2000&q=80')] bg-cover bg-center opacity-30"/><div className="relative mx-auto flex min-h-[85vh] max-w-6xl items-center px-6 py-24 md:px-12"><div className="max-w-3xl"><p className="mb-8 text-xs uppercase tracking-[.4em] text-white/50">One country · three landscapes</p><h2 className="text-balance text-5xl font-medium leading-[.95] tracking-[-.045em] md:text-7xl">From pine forests to golden dunes, Mongolia changes the rhythm of your day.</h2><p className="mt-8 max-w-xl text-base leading-8 text-white/65">Choose a base for the landscape you want to wake up in. Keep the same feeling: warm hospitality, open space and a closer connection to the land.</p></div></div></section>}
